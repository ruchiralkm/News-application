import { Routes } from '@angular/router';
import { TopheadlinesComponent } from './topheadlines/topheadlines.component';

export const routes: Routes = [{ path: '', component: TopheadlinesComponent }];
